import { useEffect, useState } from "react";
import { Link, useLocation, useNavigate } from "react-router-dom";
import { LockKeyhole, ShieldCheck } from "lucide-react";
import { useAuth } from "../../context/AuthContext";
export default function AdminLogin(){const navigate=useNavigate();const location=useLocation();const{login,user,isAuthenticated,loading:authLoading}=useAuth();const[identifier,setIdentifier]=useState("");const[password,setPassword]=useState("");const[loading,setLoading]=useState(false);const[error,setError]=useState("");useEffect(()=>{if(!authLoading&&isAuthenticated&&user?.role==="admin")navigate("/admin",{replace:true})},[authLoading,isAuthenticated,user,navigate]);const submit=async e=>{e.preventDefault();setError("");if(!identifier.trim())return setError("Enter the admin email or phone number.");if(!password)return setError("Enter the admin password.");try{setLoading(true);const d=await login(identifier.trim(),password);if(d.user?.role!=="admin")throw new Error("This account does not have admin access.");navigate(location.state?.from||"/admin",{replace:true})}catch(err){setError(err.message||"Admin login failed.")}finally{setLoading(false)}};return <main className="min-h-screen bg-brand-cream px-4 py-10 sm:py-16"><div className="mx-auto max-w-md"><Link to="/" className="mb-6 inline-flex text-sm font-bold text-brand-green">← Back to Home Foods</Link><div className="rounded-[2rem] bg-white p-6 shadow-sm ring-1 ring-black/5 sm:p-8"><div className="text-center"><div className="mx-auto grid h-16 w-16 place-items-center rounded-2xl bg-brand-green text-white"><ShieldCheck size={30}/></div><p className="mt-5 text-xs font-extrabold uppercase tracking-[0.2em] text-brand-gold">Home Foods</p><h1 className="mt-2 text-3xl font-black">Admin Login</h1><p className="mt-2 text-sm text-black/50">Sign in to manage products, orders and store settings.</p></div>{error&&<div className="mt-6 rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm font-medium text-red-700">{error}</div>}<form onSubmit={submit} className="mt-7 space-y-4"><div><label className="mb-1.5 block text-sm font-semibold">Email or Phone</label><input value={identifier} onChange={e=>setIdentifier(e.target.value)} autoComplete="username" placeholder="Admin email or phone" className="w-full rounded-xl border border-black/10 px-4 py-3.5 outline-none transition focus:border-brand-green"/></div><div><label className="mb-1.5 block text-sm font-semibold">Password</label><div className="relative"><LockKeyhole size={17} className="absolute left-4 top-1/2 -translate-y-1/2 text-black/35"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" placeholder="Admin password" className="w-full rounded-xl border border-black/10 py-3.5 pl-11 pr-4 outline-none transition focus:border-brand-green"/></div></div><button disabled={loading} className="w-full rounded-xl bg-brand-green px-4 py-3.5 font-extrabold text-white transition hover:bg-brand-green-dark disabled:cursor-not-allowed disabled:opacity-60">{loading?"SIGNING IN...":"SIGN IN AS ADMIN"}</button></form><div className="mt-6 rounded-xl bg-brand-cream p-4 text-center text-xs leading-5 text-black/50">Admin access is controlled by the user's backend role. Customer accounts cannot access the admin dashboard.</div></div></div></main>}
