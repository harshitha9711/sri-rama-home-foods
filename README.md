# Home Foods — Phase 2
Run `npm install` then `npm run dev`.
This phase contains the multi-page customer frontend, category routes, Veg/Non-Veg pickle separation, product variants, search/sort, working cart state, checkout UI and reusable components. Final client products are placeholders until supplied.
